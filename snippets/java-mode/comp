# -*- mode: snippet -*-
# name: comp
# key: comp
# --
static class Complicate implements Comparator<edge> {
  public int compare(edge o1, edge o2) {
    if (o1.cost == o2.cost) return 0;
    int i = o1.cost > o2.cost ? -1 : 1;
    return i;
  }
}